name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          registry-url: "https://registry.npmjs.org"
      # Trusted Publishing via OIDC requires npm >= 11.5.1. Node 20 ships
      # with npm 10.x, so upgrade the global npm CLI before publishing.
      - run: npm install -g npm@latest
      - run: bun install --frozen-lockfile
      - run: bun run typecheck
      # Gateway tests (test:bun) use Bun.serve and require the bun runtime;
      # they run locally via `bun run check` but are skipped in CI.
      - run: bun run test
      - run: bun run build
      - run: npm publish
